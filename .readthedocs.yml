version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "mambaforge-4.10"

sphinx:
  configuration: docs/conf.py

submodules:
  include: all

conda:
  environment: environment.yml

python:
  install:
    - method: pip
      path: .

